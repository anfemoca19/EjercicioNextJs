/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = nextConfig

module.exports = {
  images: {
    domains: ['images.unsplash.com'], // Agrega aquí cualquier otro dominio externo si es necesario
  },
};